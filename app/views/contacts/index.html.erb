<h2>Contacts</h2>
<ul>
  <% @contacts.each do |contact| %>
    <li><%=contact.first_name + " " + contact.last_name + " | " + contact.number%></li>
    <%= link_to "Edit Contact", edit_contact_path, method: :get %>
  <%end%>
</ul>

<%= link_to "Add Contact", new_contact_path, method: :get %>
